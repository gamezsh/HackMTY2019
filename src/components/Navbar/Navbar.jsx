import React from 'react';
import { NavbarContainer } from './styled';

const Navbar = props => {
  return (
    <NavbarContainer>
      <p> navbar </p>
    </NavbarContainer>
  );
};

export default Navbar;